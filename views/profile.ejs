<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Home</title>
        <meta name="description" content="">
        <link rel="stylesheet" type="text/css" href="/main.css">
    </head>
    <body>
        <p><a href="../../">return home</a></p>
        <h1><%= user.username %></h1>
        <p>purse <b><%= user.purse %></b></p>
        <p>networth <b><%= (Number(user.melons) * 14.3) + user.purse %></b></p>
        <p>melons <b><%= user.melons %></b></p>
        <p>land <b><%= user.land %>m2</b></p>
    
    </body> 
</html>