<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Home</title>
        <meta name="description" content="">
        <link rel="stylesheet" type="text/css" href="/main.css">
    </head>
    <body>
        <p><a href="./">Return</a></p>
        <h1>Sell machines</h1>
        <form method="post" action="sellingMachine">
        <% if(machines.length == 0) { %>
            <b class="error">You have no machines to sell!</b>
        <% } else { %>
            <% for(machine in machines) { %>
                <p>Sell <%= machines[machine].name %> for <%= machines[machine].price %></p>
                <button name="machine" value="<%= machines[machine].id %>">Sell</button>
        <% } } %>
        </form> 
    </body>
</html>